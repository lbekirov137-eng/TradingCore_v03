"""
Совместимый путь импорта для live_paper_run.

ЗДЕСЬ БЫЛА ВТОРАЯ КОПИЯ РЕАЛИЗАЦИИ. Файл был побайтово идентичен
корневому live_paper_run.py, и это ровно та ситуация, которая обязана
была разойтись — и разошлась: комментарий о единицах измерения
RISK_PERCENT (0.1 == 0.1%, а не 10%) появился только в корневом файле,
тогда как здесь остался код без этого предупреждения. Две копии
торгового кода с расходящимися пояснениями к риск-параметру — прямой
путь к ошибке в сто раз.

Канонической признана КОРНЕВАЯ реализация, потому что:
  - именно её импортирует рабочий цикл (paper_live_loop.py:22);
  - на этот модуль ссылается запуск `python live_paper_run.py`;
  - в ней актуальные пояснения к единицам риска.

На этот модуль не ссылался никто (проверено по всему репозиторию,
включая Dockerfile и railway.toml), но файл не удаляется: он остаётся
рабочим путём импорта на случай внешних ссылок. Теперь это тонкий
re-export без собственной логики, поэтому разойтись двум путям больше
нечем.

Циклического импорта не возникает: api/providers/__init__.py пуст, а
корневой live_paper_run импортирует api.providers.binance_public_market_provider,
то есть другой модуль пакета, а не этот.
"""

from live_paper_run import (  # noqa: F401
    CANDLE_LIMIT,
    PAPER_BALANCE,
    RISK_PERCENT,
    SYMBOL,
    TIMEFRAME,
    build_live_context,
    build_report,
    main,
)

__all__ = [
    "CANDLE_LIMIT",
    "PAPER_BALANCE",
    "RISK_PERCENT",
    "SYMBOL",
    "TIMEFRAME",
    "build_live_context",
    "build_report",
    "main",
]
